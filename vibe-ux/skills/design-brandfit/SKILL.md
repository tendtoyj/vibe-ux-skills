---
name: design-brandfit
description: 다른 곳에서 가져온 레퍼런스 DESIGN.md(디자인 시스템 문서)의 시각 표현은 그대로 유지하고, 메타정보·서사·보이스·카피만 우리 브랜드에 맞게 변환하는 스킬. docs/에서 DESIGN.md와 brandvoice.md를 찾아 gap을 분석한 뒤, 색·간격·타이포 같은 시각 토큰은 손대지 않고 이름·내러티브·Voice&Tone·페르소나·UI 카피를 브랜드 보이스에 맞춰 다시 쓴다. 본문은 영어로, 실제 서비스에 들어갈 한국어 예시 문장·표현만 한국어로 보존한다. 사용자가 "가져온 DESIGN.md 우리 걸로 바꿔줘", "디자인 시스템 브랜드화", "DESIGN.md를 브랜드 보이스에 맞게", "레퍼런스 디자인 가져와서 우리 톤으로", "design.md 변환"이라고 할 때 사용한다. Vibe UX 기획 플로우에서 brand-voice 다음, 프로토타이핑·바이브 코딩으로 넘어가기 직전의 디자인 시스템 정비 단계다.
---

# Design Brandfit

다른 서비스/디자인 시스템에서 **가져온 레퍼런스 `DESIGN.md`** 를 우리 것으로 만든다. 핵심 원칙은 하나다: **시각 표현(색값·간격·타이포·그림자·모션 수치)은 검증된 기반이므로 그대로 유지하고, 그 위에 얹힌 메타·서사·보이스·카피만 우리 브랜드(`docs/brandvoice.md`)에 맞게 변환한다.** 결과 문서의 본문은 영어로 쓰되, 실제 서비스 화면에 들어갈 한국어 카피·표현은 그 톤이 곧 브랜드의 문체이므로 한국어 그대로 보존한다.

이 스킬은 Vibe UX 기획 플로우에서 **brand-voice 다음, 프로토타이핑/바이브 코딩 직전**의 디자인 시스템 정비 단계다.

## 입력 / 산출물

- **입력:**
  - `docs/DESIGN.md` — 외부에서 가져온 레퍼런스 디자인 시스템 문서. (Google Stitch DESIGN.md 9-섹션 포맷 + 필요 시 Voice/Narrative/Personas/States/Motion 등 확장 섹션을 포함하는 경우가 많다.)
  - `docs/brandvoice.md` — brand-voice 스킬 산출물. 확정 서비스명·보이스·톤·금지선이 들어 있다.
- **산출물:** 변환된 `docs/DESIGN.md` (같은 파일을 제자리 갱신).

### 시작 시
1. `docs/DESIGN.md`가 있는지 확인한다. 없으면 "변환할 레퍼런스 DESIGN.md가 docs/에 없어요. 먼저 레퍼런스를 가져다 두실래요?"라고 안내하고 멈춘다.
2. `docs/brandvoice.md`가 있는지 확인한다. 없으면 "브랜드 보이스 기준이 없으면 변환의 방향을 잡을 수 없어요. brand-voice 스킬을 먼저 돌릴까요?"라고 묻는다.
3. 두 파일을 모두 읽은 뒤 Step 1로 간다.

---

## Step 1 — 두 문서 읽고 매핑

`DESIGN.md`와 `brandvoice.md`를 끝까지 읽는다. 읽으면서 머릿속에 매핑한다:

- `brandvoice.md`의 **핵심 성격·톤 스펙트럼·금지선·Vocabulary·Voice 샘플**이 무엇인지.
- `DESIGN.md`의 각 섹션이 **순수 시각인지 / 시각이지만 근거가 브랜드에 묶였는지 / 순수 메타·콘텐츠인지**.

이 매핑이 다음 단계 gap 분석의 재료다.

---

## Step 2 — Gap 분석 (먼저 보여주고 합의)

변환에 들어가기 전에, `DESIGN.md`를 **세 레이어로 분류**해 사용자에게 gap을 보여준다. 바로 덮어쓰지 않는다.

| 레이어 | 처리 | 전형적 위치 |
|--------|------|-------------|
| **순수 시각 (그대로 유지)** | 손대지 않음 | 색값, 타이포 스케일/weight/line-height, 간격/그리드, 컴포넌트 수치, 그림자, 반응형, 모션 duration/easing, 아이콘 *스타일 규칙*(stroke·사이즈·채움) |
| **시각이지만 '의미·근거'가 브랜드에 묶임** | 수치는 유지, 서사·명명·근거만 재해석 | 분위기 서사(§1), 색의 *이름/스토리*, Principles, 시그니처 모션 *설명* |
| **하이브리드 — 기술적 필요로 추천/치환** | 수치는 유지, 구체 선택만 추가/치환 (명시·확인) | **아이콘 라이브러리**(스타일 수치 유지 + 라이브러리 1개 추천), **폰트 패밀리**(스케일·weight는 신성 유지 + 패밀리는 기술적 필요시 치환) |
| **순수 메타·콘텐츠 (전면 교체)** | 브랜드로 변환 | frontmatter, Voice & Tone, Brand Narrative, Personas, States의 *카피 예시*, 모든 CTA·카피 샘플 |

그런 다음 **brandvoice와 정면 충돌하는 지점**, 그리고 **기술적으로 메워야 할 gap**을 우선순위로 짚는다. 흔한 충돌·gap:
- **이모지 정책** — 레퍼런스가 "이모지 허용"인데 brandvoice가 금지하면 충돌.
- **CTA 톤** — 레퍼런스의 명령형 vs brandvoice의 제안형/금지조.
- **Forbidden phrases** — 레퍼런스 금지어를 brandvoice의 금지선으로 교체.
- **Voice & Tone / Narrative / Personas** — 거의 항상 100% 교체 대상.
- **브랜드 고유 명명** — 레퍼런스의 색 토큰명·시스템명·제품명(예: `carrot-*`, 특정 서비스명)은 메타이므로 변환 대상.
- **폰트 가용성·한글 커버리지** — 레퍼런스 폰트가 무료/라이선스 웹폰트로 없거나 한글을 지원하지 않으면(Vibe UX는 항상 한국어 카피가 실제 화면에 들어간다) 한글 지원 웹폰트로 치환이 필요한 gap.
- **아이콘 스펙이 특정 라이브러리를 강제** — 레퍼런스가 duotone·멀티 weight를 요구하면 Phosphor만 충족하는 등, 스타일 스펙 자체가 라이브러리를 좁힌다.

이미 **정렬되어 손댈 필요 없는 부분**(예: 둘 다 무이모지, 둘 다 차분한 elevation)도 함께 알려줘 사용자가 변환 범위를 신뢰하게 한다.

gap 분석을 짧게 보고하고, "이대로 변환할까요? 아니면 색·톤 같은 결정부터 합의할까요?"라고 한 번 확인한 뒤 Step 3로 간다.

---

## Step 3 — 변환 (시각 유지 / 메타·카피 변환)

합의되면 `DESIGN.md`를 다시 쓴다. 규칙:

**유지 — 한 글자도 바꾸지 않는다**
- 모든 색 **값**(hex/rgba), 타이포 사이즈·weight·line-height, 4px 간격 체계, radius 스케일, 그림자 정의, 브레이크포인트·터치 타깃, 모션 duration/easing 토큰 값.
- 컴포넌트 수치, 아이코노그래피 규칙, Document Policies 구조.

**변환 — 브랜드로 다시 쓴다**
- **frontmatter:** `id`, `name`, `homepage`, `logo`, `ds.name`/`description`, `category`를 우리 서비스로. (verified 날짜는 오늘.)
- **명명:** 레퍼런스 고유 토큰명(예: `carrot-*` → `accent-*`), 색 이름(예: "Karrot Orange" → 우리 브랜드 색명), 디자인 시스템명, 제품명 전부 교체. *렌더링은 동일하되 이름만 탈(脫)레퍼런스화.*
- **서사(§1, Brand Narrative):** 레퍼런스의 히스토리·창업 연혁·지표를 우리 제품 **철학·메타포**로 다시 쓴다. **없는 사실(투자·사용자 수·창업자)을 지어내지 않는다** — 신규 서비스면 철학 서술로만.
- **Voice & Tone:** brandvoice의 컨텍스트 표·금지선·Voice 샘플로 전면 교체.
- **Personas:** 레퍼런스 페르소나 → 우리 서비스의 사용자 아키타입(가상임을 명시).
- **States/Principles/Motion 설명:** 구조·수치는 유지, **근거와 예시 카피만** 브랜드로 재해석. (예: spring 금지 근거를 "마켓 신뢰"에서 우리 톤으로.)

**하이브리드 — 수치 유지 + 근거 재해석**
- Principles, 시그니처 모션, Whitespace 철학처럼 "값은 맞지만 *왜*가 레퍼런스에 묶인" 항목은 수치를 두고 설명 문장만 바꾼다.

**아이콘 라이브러리 추천 (스타일 수치는 유지, 구체 라이브러리만 추가):**
레퍼런스의 아이콘 **스타일 규칙**(stroke 굵기·사이즈·아웃라인/채움·corner)은 시각이므로 그대로 둔다. 다만 레퍼런스가 보통 *구체 라이브러리*는 안 정하는데 바이브 코딩은 `import`할 패키지가 필요하므로, **brandvoice 성격 × 레퍼런스 아이콘 스타일 스펙**에 맞춰 아래 4개 중 1개를 근거 한 줄과 함께 추천한다. (자동 확정하지 않고 Step 2 gap에서 확인.)

| 라이브러리 | 성격 | 잘 맞는 브랜드 |
|---|---|---|
| **Lucide** | 균일 2px 아웃라인, 중립·깔끔, ~1500개. 사실상 디폴트 | 대부분의 무난·신뢰 톤 |
| **Phosphor** | 6 weight(thin~fill·duotone), 둥근 join, 방대 | 따뜻함·친근·플레이풀 |
| **Heroicons** | outline/solid/mini, 약간 기하학적, ~300 | Tailwind 스택, 정갈 |
| **Radix** | 15px 미니멀·기하, ~300 | 밀도 높은 pro/dev 툴, 정밀 |

레퍼런스 스타일 스펙이 라이브러리를 강제하면(예: duotone·멀티 weight → Phosphor만 충족) 그 제약을 명시한다. 넷 다 MIT/오픈.

**폰트 패밀리 치환 (기술적 필요 한정 예외):**
타이포의 **스케일·weight·line-height·letter-spacing은 신성하게 유지한다.** 다만 폰트 *패밀리*는 두 조건일 때만 치환한다: ① 레퍼런스 폰트가 무료/라이선스 웹폰트로 존재하지 않거나, ② 한글을 지원하지 않을 때(Vibe UX는 한국어 카피가 실제 화면에 들어가므로 한글 지원이 사실상 필수). 치환 시 레퍼런스 폰트의 **personality(기하학/휴머니스트/세리프)에 가장 가까운 한국어 웹폰트로 매칭**하고, 변환 노트에 "레퍼런스 X → 한글 지원 위해 Y로 치환"을 명시한 뒤 Step 2 gap에서 확인한다.

| 레퍼런스 느낌 | 1순위 | 대안 |
|---|---|---|
| Inter/Geist/기하학 sans (가장 흔함) | **Pretendard** (Inter류 Latin + 우수한 한글, variable, OFL) | Wanted Sans, SUIT |
| 최대 호환·안전 baseline | **Noto Sans KR** (구글 호스팅, 전 글리프, 다중 weight) | — |
| 중립·휴머니스트 UI | **Spoqa Han Sans Neo** | Noto Sans KR |
| 세리프/에디토리얼 | **Noto Serif KR** | 나눔명조, Gowun Batang |

기본은 **Pretendard 우선, 케이스별 조정**(세리프/특수 personality면 매칭 우선, 최대 호환 필요하면 Noto). 폰트 스택은 `Pretendard, -apple-system, ..., sans-serif`처럼 fallback과 시스템 폰트까지 명시하고 라이선스(OFL)를 한 줄 적는다.

**색 결정 원칙:** 사용자가 "시각 유지"를 요청한 이상 **색값은 바꾸지 않는다.** 색의 *이름·근거*만 브랜드 메타포로 재해석한다. 브랜드 톤과 색이 긴장 관계면(예: 차분한 보이스 vs 비비드한 액센트) "액센트는 의도적으로 희소" 같은 원칙으로 관리하도록 문서에 명시하고, 색 자체를 바꾸고 싶은지는 **별도로** 묻는다.

---

## Step 4 — 언어 정책 (본문 영어 + 한국어 카피 보존)

변환된 문서는 **본문(설명·구조·서사)을 영어로** 쓴다. 단, **실제 서비스에 들어갈 한국어 예시 문장·표현은 그 자체가 브랜드의 문체이므로 한국어 그대로 둔다.**

**한국어로 보존하는 것 (실제 문체):**
- Voice 샘플 — 온보딩·빈 상태·성공·에러·푸시·랜딩 등 실제 카피 문장.
- Voice & Tone 컨텍스트 표의 **괄호 안 예시 문구**.
- **Forbidden phrases** — 실제 금지 문구 (분류 라벨은 영어).
- **CTA 레이블** — 버튼에 실제로 들어갈 한국어(예: `오늘 칸 열기`).
- **States 표의 실제 UI 카피 문자열**.
- 존댓말 어미·화법(`~해요`, `~할까요` 등 보이스 정의에 필요한 표현).
- **브랜드 고유 어휘** — 서사 본문에선 영어로 풀되 첫 등장 시 `frame (칸)`처럼 한국어 병기.

**영어로 쓰는 것:** §1~9 본문, Voice/Narrative/Principles/Personas/States/Motion의 설명·근거, treatment 서술, 변환 노트·footer. 아이콘 라이브러리명(Lucide 등)·폰트명(Pretendard 등)은 고유명사이므로 영어 표기 그대로 둔다(병기 불필요).

**고유명사 병기:** 서비스명은 첫 등장 시 `BrandName (브랜드명)`, 페르소나 이름은 `수아 (Sua)`처럼 한글+로마자 병기. (병기는 문체 보존이 아니라 고유명사 표기 규칙.)

> 변환 노트(주석 블록)와 footer에는 "시각 토큰은 레퍼런스에서 유지, 메타·보이스는 brandvoice 기반 재작성", voice 샘플의 검증 상태(신규 서비스면 미검증 기획 카피), 색 결정 근거를 영어로 남긴다.

---

## Step 5 — 저장 & 보고

변환 내용을 **`docs/DESIGN.md`에 제자리 저장한다.** ("저장할까요?"라고 묻지 않는다 — 저장이 결과물이다.)

저장 후 짧게 보고한다:
- **유지한 것** (시각 토큰 한 줄 요약),
- **변환한 것** (섹션별: frontmatter·명명·Narrative·Voice·Personas·States),
- **추천·치환한 것** (추천한 아이콘 라이브러리, 폰트 치환 내역: 레퍼런스 X → Y와 이유),
- **한국어로 보존한 카피**,
- **짚어둘 판단** (특히 색값 유지 결정, 미검증 voice 샘플 등),
- 이어서: "이 DESIGN.md를 기준으로 프로토타이핑/바이브 코딩 단계의 UI 카피와 화면 톤을 잡을 수 있어요."

## 원칙
- **시각은 신성하다.** 색값·수치는 검증된 기반 — 사용자가 명시적으로 바꾸자고 하기 전엔 손대지 않는다. **단 폰트 패밀리는 유일한 예외**로, 웹 가용성·한글 커버리지라는 기술적 필요에 한해 personality를 보존하며 치환할 수 있다(명시·확인 필수). 스케일·weight·line-height 등 수치는 폰트에서도 여전히 신성하다.
- **빈 곳은 메우되 덮어쓰지 않는다.** 아이콘 라이브러리·폰트 패밀리처럼 레퍼런스에 비어 있고 바이브 코딩에 필요한 결정은 추천으로 채우되, 자동 확정하지 않고 gap에서 확인한다.
- **사실을 지어내지 않는다.** 레퍼런스의 연혁·지표를 우리 것으로 위조하지 않는다. 신규 서비스는 철학 서술로.
- **문체는 보존한다.** 실제 한국어 카피는 번역하지 않는다 — 그게 브랜드의 목소리다.
- **gap을 먼저 보여준다.** 덮어쓰기 전에 무엇을 유지/변환하는지 합의한다.
